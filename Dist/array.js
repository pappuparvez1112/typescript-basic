"use strict";
const names = ["abul", "kabul", "babul"];
const rollNumber = [55, 33, 44];
rollNumber.map(roll => roll.toFixed);
names.map(name => name.indexOf);
const student = ["abul", "kabul", "babul", 77, true];
student.push('parvez');
const boyfriends = ["abul", true];
console.log(boyfriends);
